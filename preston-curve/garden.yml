kind: Module
type: container
name: model-orchestrator
services:
  - name: r-instance
    ports:
      - name: http
        containerPort: 3000
    devMode:
      sync:
        - target: /app
          mode: two-way
      
# tasks:
#   - name: generate-paper
#     # command: ["dagster", "pipeline", "execute", "-f", "main.py", "--preset", "unittest"]
#     artifacts:
#       - source: /app/*.parquet
#         target: results
# tests:
#   - name: test-alec
#     command: ["dagster", "pipeline", "execute", "-f", "main.py", "--preset", "unittest"]
